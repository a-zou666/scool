#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n,m,ans;
void solve(){
    cin>>n;
    for(ll i=0;i<n;i++){
        cin>>m;
        ans^=m;
    }
    cout<<ans;

}

signed main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    solve();
}
